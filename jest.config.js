module.exports = {
  roots: ['test'],
  collectCoverage: true, // Collect the coverage information while executing the test
  coverageDirectory: 'test/unit/coverage' // The directory where Jest output its coverage files
}
